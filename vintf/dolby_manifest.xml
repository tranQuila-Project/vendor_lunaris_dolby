<!-- android.hardware.media.c2 is declared by the device tree manifest.
     This file intentionally left without it to avoid duplicate HAL entries. -->
<manifest version="2.0" type="device">
</manifest>
